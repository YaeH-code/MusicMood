<form method="post" action="{{ path('app_admin_mood_delete', {'id': mood.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mood.id) }}">
    <button class="btn mb-5 btn_act2">Supprimer</button>
</form>
