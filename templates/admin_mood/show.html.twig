{% extends 'base.html.twig' %}

{% block title %}Mood{% endblock %}

{% block body %}
    <h1>Mood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mood.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mood.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_mood_index') }}">back to list</a>

    <a href="{{ path('app_admin_mood_edit', {'id': mood.id}) }}">edit</a>

    {{ include('admin_mood/_delete_form.html.twig') }}
{% endblock %}
