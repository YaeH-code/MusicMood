{% extends 'base.html.twig' %}

{% block title %}Mood{% endblock %}

{% block body %}
    <h1 class="mt-5 mb-5 text-center">Mood</h1>
        <div class="container mood_div">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ mood.name }}</td>
                </tr>
            </tbody>
        </table>

        <div class="return">
            <a href="{{ path('app_admin_mood_index') }}">&lsaquo; &lsaquo; Retour à la liste </a>
        </div>
        <div class="edit">
        <a href="{{ path('app_admin_mood_edit', {'id': mood.id}) }}">Modifier</a>
        </div>
    <div class="text-center">
        {{ include('admin_mood/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
