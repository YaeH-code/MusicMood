{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
{{ form_start(form) }}
<div class="form-group mb-5">
    {{ form_widget(form) }}
</div>
    <button class="btn mb-5 btn_act2">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}